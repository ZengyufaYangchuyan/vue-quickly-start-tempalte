const webpack = require('webpack');

const config = [

];

/**
 * 获取插件配置
 * @param {Boolean} isPorduction 是否为产品
 */
const getConfig = (isPorduction) => {

    if (isPorduction) {

    }

    return config;
};

module.exports = getConfig;